
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.aaron.videoplay.entity
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/video_play?serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 1000MB
logging:
  level:
     com.aaron.videoplay.mapper: debug



